
  # Create Responsive Layout (Copy)

  This is a code bundle for Create Responsive Layout (Copy). The original project is available at https://www.figma.com/design/SA5JkBxYqvGzJMzbahbK5n/Create-Responsive-Layout--Copy-.

  ## Running the code

  Run `npm i` to install the dependencies.

  Run `npm run dev` to start the development server.
  